`timescale 1ns / 1ps

module UPDOWNcounter( clk,rst,mode,Q);
    input clk,rst,mode;
    output reg[3:0] Q;
    
    
    always @(posedge clk)
    begin
    if (rst)
    Q <= 4'b0000;
    
    else if ( mode )
    Q <= Q + 1'b1;
    
    else 
    Q <= Q - 1'b1;
    end
endmodule
